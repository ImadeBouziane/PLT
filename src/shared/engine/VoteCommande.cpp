#include "VoteCommand.h"
/*

namespace engine {
         VoteCommand::~VoteCommand(state::PlayerID player) {}


         bool VoteCommand::VoteCommand(state::PlayerID){
            
         std::cout << "Joueur " << static_cast<int>(playerID) << ", votez-vous oui (1) ou non (0) ? ";
         int vote;
         std::cin >> vote;

         return vote == 1; // Retourne true si le joueur vote oui (1), false autrement
    }

        
   void Game::prepareVote() {
    engine::VoteCommand voteCmd; // Créez une instance de VoteCommand
    int yesVotes = 0;
    int noVotes = 0;

    for (auto& player : listPlayers) {
        // Supposons que PlayerID est accessible pour chaque objet player
        state::PlayerID playerID = player.getPlayerID(); // Méthode fictive pour obtenir PlayerID
        if (voteCmd.askForVote(playerID)) {
            yesVotes++;
        } else {
            noVotes++;
        }
    }

    if (yesVotes > noVotes) {
        
    } else {
        // La majorité a voté non ou égalité
        // Gérez ce cas si nécessaire
    }
}
         }

*/